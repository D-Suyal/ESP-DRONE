# Third-party notices

## Bosch Sensortec (SensorAPI)

Bosch BMI270 and BMP581 driver code is included under BSD-3-Clause:

- `components/drivers/i2c_devices/bmi270/bosch/` — see `LICENSE` in that directory
- `components/drivers/i2c_devices/bmp581/bosch/` — see `LICENSE` in that directory
